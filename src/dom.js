// supprimer la ligne suivante
var workspace = null;
// récupérez la div "workspace" par son id
// enregistrez le dans une variable workspace
// votre code

// affichez dans la console le nombre de noeuds que contient le workspace
// votre code

// récupérez le contenu (html) du premier paragraphe contenu dans workspace
// vous ne devez pas ajouter d'id
// supprimez la ligne suivante
var p = null;
// sauvegardez le résultat dans la variable p
// votre code

console.log(p);

// ajouter le code suivant : "<h3>Hello JS world !</h3>" au premier élément du
// votre code

// completez la fonction runChrono pour qu'elle affiche un décompte
// de n à 0, dans une div (id: counter), placée sous la balise h3 précédemment ajoutée
// vous ne devez pas touchez code HTML, et le h3 doit continuer à s'afficher
function runChrono(n) {
  // votre code
}

console.log(runChrono(3));

// changez la couleur de fond du body : #992323
// ne modifiez pas le code HTML de la page directement
// votre code

// changez la couleur du texte du body : #fff
// votre code
